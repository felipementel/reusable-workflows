name: 'Docker Setup Buildx Action (Wrapper)'
description: 'Wrapper composite action for docker/setup-buildx-action. Update the version only here.'

inputs:
  version:
    description: 'Buildx version. (eg. v0.3.0)'
    required: false
  driver:
    description: 'Sets the builder driver to be used'
    required: false
    default: 'docker-container'
  driver-opts:
    description: 'List of additional driver-specific options. (eg. image=moby/buildkit:master)'
    required: false
  buildkitd-flags:
    description: 'BuildKit daemon flags'
    required: false
  buildkitd-config:
    description: 'BuildKit daemon config file'
    required: false
  buildkitd-config-inline:
    description: 'Inline BuildKit daemon config'
    required: false
  use:
    description: 'Set the current builder instance'
    required: false
    default: 'true'
  name:
    description: 'Builder instance name'
    required: false
  endpoint:
    description: 'Optional address for docker socket or context from docker context ls'
    required: false
  platforms:
    description: 'Fixed platforms for current node. If not empty, values take priority over the detected ones'
    required: false
  append:
    description: 'Append additional nodes to the builder'
    required: false
  cache-binary:
    description: 'Cache buildx binary to GitHub Actions cache backend'
    required: false
    default: 'true'
  cleanup:
    description: 'Cleanup temp files and remove builder at the end of a job'
    required: false
    default: 'true'

outputs:
  name:
    description: 'Builder instance name'
    value: ${{ steps.buildx.outputs.name }}
  driver:
    description: 'Builder instance driver'
    value: ${{ steps.buildx.outputs.driver }}
  platforms:
    description: 'Available platforms'
    value: ${{ steps.buildx.outputs.platforms }}
  nodes:
    description: 'Builder nodes metadata'
    value: ${{ steps.buildx.outputs.nodes }}

runs:
  using: 'composite'
  steps:
    - name: 🐳 Set up Docker Buildx
      id: buildx
      uses: docker/setup-buildx-action@v4
      with:
        version: ${{ inputs.version }}
        driver: ${{ inputs.driver }}
        driver-opts: ${{ inputs.driver-opts }}
        buildkitd-flags: ${{ inputs.buildkitd-flags }}
        buildkitd-config: ${{ inputs.buildkitd-config }}
        buildkitd-config-inline: ${{ inputs.buildkitd-config-inline }}
        use: ${{ inputs.use }}
        name: ${{ inputs.name }}
        endpoint: ${{ inputs.endpoint }}
        platforms: ${{ inputs.platforms }}
        append: ${{ inputs.append }}
        cache-binary: ${{ inputs.cache-binary }}
        cleanup: ${{ inputs.cleanup }}
